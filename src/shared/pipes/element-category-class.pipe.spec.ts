import { ElementCategoryClassPipe } from './element-category-class.pipe';

describe('ElementCategoryClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementCategoryClassPipe();
    expect(pipe).toBeTruthy();
  });
});
